import  { createElement } from 'lwc';
import helloWorld from 'c/c/helloWorld';

describe('c-hello-world', () => {
    afterEach(() => {
        while(document.body.firstChild) {
            document.body.removeChild()
        }
    };

    it('displays greeting', () => {
        const element =
    })
})